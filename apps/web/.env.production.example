# ===========================================================================
# Harmony Web (Next.js) — production environment template
# Set this in Vercel (Project Settings → Environment Variables).
# ===========================================================================

# Public base URL of the deployed API, including the /api/v1 prefix.
# This is baked into the client bundle AND used to build the CSP allow-list.
NEXT_PUBLIC_API_URL=https://your-api.up.railway.app/api/v1
